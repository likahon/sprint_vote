{
  "name": "sprint-vote-planning-poker",
  "version": "1.0.0",
  "description": "Aplicación de Planning Poker para estimación de historias de usuario en sprints",
  "main": "server/dist/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "server:dev-db": "cd server && npm run dev-db",
    "build": "npm run server:build && npm run client:build",
    "server:build": "cd server && npm run build",
    "client:build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "clean": "rm -rf node_modules server/node_modules client/node_modules server/dist client/dist"
  },
  "keywords": [
    "planning-poker",
    "agile",
    "estimation",
    "sprint",
    "react",
    "nodejs",
    "socket.io",
    "mongodb"
  ],
  "author": "Tu Nombre",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
